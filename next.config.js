// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com'], // replace with your actual image host (e.g. cloudinary.com)
  },
};

module.exports = nextConfig;
